export default interface latestPost {
  id?: number;
  title: string;
  img: string;
  like?: number;
  description?: string;
}

export default interface post {
  id?: number;
  img: string;
  title: string;
}
