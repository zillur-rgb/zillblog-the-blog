export default interface latestPost {
  id?: number;
  title: string;
  img: string;
  like?: number;
  description?: string;
  category?: string;
}

export default interface post {
  id?: number;
  title: string;
  img: string;
  like?: number;
  description?: string;
  category?: string;
}
